<Window x:Class="WeatherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Weather App" Height="400" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Enter City Name:" FontSize="16" Margin="5" />
        <TextBox x:Name="CityTextBox" Grid.Row="1" FontSize="14" Margin="5" />
        <Button Content="Get Weather" Grid.Row="1" HorizontalAlignment="Right" Margin="5" 
                Width="100" Click="GetWeatherButton_Click" />

        <StackPanel Grid.Row="2" Margin="10">
            <TextBlock x:Name="CityNameText" FontSize="18" FontWeight="Bold" />
            <TextBlock x:Name="TemperatureText" FontSize="16" />
            <TextBlock x:Name="HumidityText" FontSize="16" />
            <TextBlock x:Name="DescriptionText" FontSize="16" />
        </StackPanel>
    </Grid>
</Window>
